#include "planning.h"

Planning::Planning(QString type, int day, int month, int hour, int min, int duration) :
_type(type), _day(day), _month(month), _hour(hour), _minute(min), _duration(duration)
{

}

